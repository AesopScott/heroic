# Heroic 1.0 Progress

## Current 1.0 Scope

1.0 means:

- complete Arcane school
- Blink, Lunge, and Teleport movement set
- one arena
- basic enemy waves
- XP, level-up, and draft progression
- win/loss run loop
- enough UI and feedback to play and tune

## Completed In Current Scaffold

- Unity `.gitignore`
- minimal Unity `Packages` and `ProjectSettings`
- XP pickup runtime
- player level runtime
- draft opening cadence with movement on even levels
- damageable death event
- enemy XP drop hook
- movement slots for keys `1`, `2`, and `3`
- first playable Blink behavior
- first playable Lunge behavior
- first playable Teleport behavior
- expanded skill ScriptableObject fields
- five-tier upgrade path model
- run build state tracking
- first-pass Arcane casters:
  - Magic Missile
  - Arcane Blast
  - Warp Pulse
  - Spell Echo support
  - Arcane Orbit
- simple upgrade choice applier
- run timer and win/loss run end state
- player death watcher
- boss death victory watcher
- camera follow
- HUD presenter hooks
- draft presenter hooks
- movement slot presenter hooks
- results presenter hooks
- pause input handler
- wave definition spawn entries
- data-driven enemy stat application
- boss spawner
- first-pass boss behavior with pulse and surge patterns
- Magic Missile projectile count, homing, and pierce fields
- Arcane Blast scatter fields
- prototype draft pool for Arcane unlocks, Arcane boosts, and movement picks
- Arcane upgrade choices can apply five-tier runtime tuning
- procedural placeholder graphics system
- visual presets for player, enemies, boss, projectiles, orbitals, and XP
- temporary visual effects for Arcane Blast, Warp Pulse, Blink, Lunge, and Teleport
- Unity editor bootstrapper for prototype scenes, prefabs, enemy assets, wave assets, and UI objects
- hit flashes and death bursts
- arena backdrop
- Unity editor readiness validator
- investor showcase mode and demo guide
- editor bootstrapper property validation for clearer generation failures
- investor showcase mode
- Unity Hub `3.19.4` installed locally
- Unity Editor `6000.5.3f1 (c2eb47b3a2a9)` installed locally
- WebGL Build Support installed locally
- Unity Personal license is active and Unity 6 imports the project successfully
- source-level C# compile passes against installed Unity assemblies
- HUD health/XP sliders added to generated prototype UI
- movement slot cooldown fill/text added to generated prototype UI
- damage application hardened against zero/negative damage
- procedural audio feedback for enemy hits/deaths, boss hits/deaths, player damage/death, XP pickup, and movement skills
- generated MainMenu scene now includes a functional start button, pitch text, controls, and procedural backdrop
- added `Heroic/Build WebGL 1.0` editor command
- readiness validator now checks build settings, MainMenu start flow, UI slider/cooldown references, prefab VFX, and prefab audio feedback
- world-space health bars for generated player, enemies, and boss
- floating combat damage numbers for generated player, enemies, and boss
- readiness validator now requires health bars and damage number emitters on generated combatants
- enemy definitions now carry visual presets so basic, fast, tank, and boss enemies read differently even when generated from a shared base prefab
- spawners apply enemy visual presets plus readable health bar/damage-number sizing at spawn time
- readiness validator now checks generated enemy definitions for expected visual presets
- readiness validator exposes machine-readable pass/fail results for automation
- WebGL builder exposes machine-readable success/failure results for automation
- batch-mode automation entrypoint added for build, validate, and WebGL generation
- `scripts/run-unity-1.0.ps1` added as the post-license one-command 1.0 runner
- real Unity scenes, prefabs, ScriptableObjects, `.meta` files, and build settings generated by Unity `6000.5.3f1`
- batch-mode `.\scripts\run-unity-1.0.ps1 -SkipWebGL` completes successfully
- batch-mode `.\scripts\run-unity-1.0.ps1` completes successfully and produces a WebGL build at `Builds/WebGL`
- level-up draft buttons now show category, skill name, and description instead of name-only labels
- local WebGL smoke server added for correct Unity `.gz` headers
- browser smoke test passed from `http://localhost:5180`
- MainMenu Start Run button now uses serialized persistent listeners and successfully enters the Game scene
- generated TMP essential resources are included so menu/HUD text renders in WebGL without missing-font warnings
- HUD health and XP bars are top-anchored so they no longer cross the player or playfield center
- HUD health and XP bars are separated into distinct rows; XP now shows an `XP current/required` counter and renders an empty/filling bar instead of always appearing full
- readiness validator now checks persistent button listeners and top-anchored HUD health/XP bars
- delayed Magic Missile echo/double-cast targeting is guarded against destroyed enemies
- browser control smoke test confirmed gameplay, movement-skill input, combat visuals, separated HUD health/XP bars, empty `XP 0/5` fill state, and a clean current-port console
- in-game objective panel now explains the demo goal, boss countdown, and next draft XP progress during gameplay
- readiness validator now requires the objective panel and its goal/boss/upgrade text references
- results screen now has a dark backdrop, centered presentation panel, stronger victory/defeat headline, run-time label, short summary copy, and clearer button labels
- readiness validator now checks the results summary text and results backdrop
- two-minute WebGL browser smoke confirmed the run reaches the polished `ARCANE WARDEN DEFEATED` victory screen at `02:00` with a clean current-port console
- XP pickup attraction is now forgiving enough for 1.0 demo reliability, with validator coverage to prevent regressions
- browser smoke confirmed XP collection opens a level-up draft, selecting a choice resumes gameplay at Level 2, and the XP counter advances cleanly
- hidden prototype demo safety hotkeys now allow F8 forced defeat and F9 forced victory during gameplay for QA/demo recovery
- browser smoke confirmed F8 reaches the polished `SPELLBOOK SHATTERED` defeat results screen with a clean current-port console
- results buttons now load scenes directly for demo safety: `Run It Back` reloads `Game`, and `Main Menu` loads `MainMenu`
- browser smoke confirmed `Run It Back` starts a clean new run and `Main Menu` returns to the title screen with a clean current-port console
- pre-demo check script added and verified: builds WebGL, starts gzip-aware local server, validates Unity WebGL headers, and prints exact URL/controls/safety keys
- investor demo runbook added with two-minute flow, talk track, and F8/F9 safety instructions
- browser smoke confirmed the `pre-demo-check.ps1` URL loads the title screen, starts gameplay, shows objective/HUD, and keeps the current-port console clean
- background music hook added for menu, gameplay, and results; it auto-loads `Assets/_Heroic/Resources/Audio/Music/HeroicDemoLoop.*` when a Suno track is dropped in
- `Shadows to Dawn.mp3` copied into the Unity Resources music slot as `HeroicDemoLoop.mp3`
- background music now retries after the first keyboard, mouse, or touch input to reduce WebGL autoplay risk
- removed documentation files from the Unity Resources music folder so only the audio asset is packaged
- Suno music brief added at `docs/music-brief-suno.md` with prompt, export guidance, and Unity asset path
- full WebGL rebuild passed after music import and resource cleanup; build log shows only `HeroicDemoLoop.mp3` from the music Resources folder is packaged
- browser smoke from `http://127.0.0.1:5202/?v=music-smoke` confirmed title load, Start Run, gameplay HUD/objective, movement input, early draft appearance, and clean current-port console
- Unity warning cleanup replaced deprecated object lookup and Rigidbody2D APIs in owned runtime/editor code
- level-up drafts now randomly present `3-5` choices instead of always using a fixed maximum
- demo safety hotkeys now work from Playing, Paused, and LevelUpDraft states, and forced run ends restore `Time.timeScale` before showing results
- full WebGL rebuild passed after warning/safety cleanup with no owned C# warnings in the checked log
- browser smoke from `http://127.0.0.1:5204/?v=safety-draft-smoke` confirmed F9 works while the draft modal is open and reaches the polished victory screen with a clean current-port console
- custom Heroic WebGL template added so builds use a branded dark shell instead of the default Unity footer frame
- project metadata now uses `Aesop Scott` as company name and `PROJECT:Heroic` as WebGL template
- latest branded WebGL smoke from `http://127.0.0.1:5205/?v=branded-shell-smoke` confirms no default Unity footer, clean title/menu view, Start Run gameplay, movement input, cooldown UI, and clean current-port console
- demo audio recovery controls added across menu, gameplay, and results: `M` toggles music and `-` / `+` adjust master volume
- latest audio-control smoke from `http://127.0.0.1:5206/?v=audio-controls-smoke` confirms menu controls are visible, audio hotkeys do not throw errors in menu/gameplay, Start Run still works, cooldown UI updates, and the current-port console stays clean
- visible generated pause overlay added for `Esc`, including resume, audio, and F8/F9 recovery prompts
- latest pause smoke from `http://127.0.0.1:5207/?v=pause-overlay-smoke` confirms Esc shows the pause panel, Esc resumes gameplay, and the current-port console stays clean
- conservative camera shake feedback added for movement activation, player damage, boss spawn, and boss death to make the 1.0 graphics/impact pass feel less floaty
- latest camera feedback smoke from `http://127.0.0.1:5208/?v=camera-shake-smoke` confirms movement inputs, cooldown UI, F9 victory result, and clean current-port console after camera shake wiring
- enemy role silhouettes strengthened in the procedural visual presets: basic outline/core, fast needle/afterimages, tank shoulders/warning core, and boss halo/cross/eye layers
- latest enemy-readability smoke from `http://127.0.0.1:5209/?v=enemy-readability-smoke` confirms Start Run, draft/resume flow, movement visuals, XP pickups, HUD, and clean current-port console after the silhouette pass
- release source-control hygiene added with `.gitattributes` for Unity YAML text assets, binary media/build payload handling, and local Unity SmartMerge configuration
- release WebGL build passed through `.\scripts\run-unity-1.0.ps1` with no owned C# compile warnings/errors
- release pre-demo check passed from `http://127.0.0.1:5211/`
- release browser smoke from `http://127.0.0.1:5211/?v=release-smoke` confirms branded menu, Start Run, movement slots/cooldowns, XP counter/fill, early draft, draft selection/resume, visible pause overlay, F9 polished victory, and clean current-port console

## Still Missing For 1.0

- No known blocker remains for the internal/investor 1.0 prototype path.
- Longer balance tuning is still needed after 1.0 for enemy pressure, Arcane upgrade values, and long-run pacing.
- Imported Suno music candidate still benefits from a presenter listen in the actual room, but recovery controls exist if the mix is wrong.

## Next Best Build Slice

1. Commit and push the validated 1.0 prototype baseline.
2. Run one presenter rehearsal using `docs/investor-demo-runbook.md`.
3. Start post-1.0 tuning for enemy pressure, procedural audio volume, spell readability, and Arcane upgrade balance.

`.\scripts\run-unity-1.0.ps1` can generate content, validate it, and run the WebGL build in batch mode through Unity `6000.5.3f1`.
